const defaultState = {
    name: 'mhy',
    newList: []
}

export default (state = defaultState, action) => {
    switch(action.type){
        // case :
        default:
            return state
    }
}
